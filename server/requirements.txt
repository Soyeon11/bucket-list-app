# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Settings / environment
pydantic==2.10.3
pydantic-settings==2.6.1

# Supabase
supabase==2.10.0
gotrue==2.10.0

# HTTP client (used by supabase-py internally)
httpx==0.27.2

# Async support
anyio==4.7.0

# Task queue (Phase 4 — structure ready, not yet wired)
celery==5.4.0
redis==5.2.1

# Utilities
python-jose[cryptography]==3.3.0   # JWT decoding / verification
python-multipart==0.0.20           # Form data / file upload
email-validator==2.2.0

# Video processing (Phase 4)
moviepy==1.0.3                     # FFmpeg wrapper for clip composition
Pillow==10.4.0                     # Image compositing (intro/outro frames, text overlay)
ffmpeg-python==0.2.0               # Thin FFmpeg subprocess wrapper (loudnorm, probing)
ffmpeg-normalize==1.31.0           # CLI helper for loudness normalization (optional, ffmpeg loudnorm used directly)

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2                      # also used as async test client
